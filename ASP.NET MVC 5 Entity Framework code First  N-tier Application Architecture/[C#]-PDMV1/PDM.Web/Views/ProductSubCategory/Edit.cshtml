@model  PDM.Model.ProductSubCategory

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title" id="categoryModalLabel">Edit Product Sub Category</h4>
</div>
<div>
    @using (Ajax.BeginForm("Edit", "ProductSubCategory", FormMethod.Post,
                    new AjaxOptions
                    {
                        InsertionMode = InsertionMode.Replace,
                        HttpMethod = "POST",
                        OnSuccess = "OnSuccess",
                        UpdateTargetId = "tableBody",
                        LoadingElementDuration = 1000
                    }, new { @id = "editsubCategoryForm" }))
    {
        <div class="modal-body">
            <div class="form-group">
                @Html.HiddenFor(m => m.ProductCategoryID)
                @Html.HiddenFor(m => m.ProductSubCategoryID)
                @Html.HiddenFor(m => m.rowguid)
                <label for="category-name" class="control-label">Category Name:</label>
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Name)
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <button type="submit" value="submit" class="btn btn-primary">Save & Close</button>
        </div>
    }
</div>
